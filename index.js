/*

var title = [];
var data  = [];
document.getElementById('create').addEventListener('click',()=>{
    let x = document.getElementById('newtitle').innerText;
    let y = document.getElementById('newcontent').innerText;
    let z = document.getElementById('mahaan');
    let creator = document.createElement('div');
    creator.setAttribute('id',x);
    creator.setAttribute('class','newnotes');
    let textnode = document.createTextNode(y);
    creator.appendChild(textnode);
    z.appendChild(creator);
    z.style.display = "block";
})

*/